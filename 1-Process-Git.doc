# 1.0 - Proceso/flujo de trabajo básico en git
git add 'nombre_archivo'/. -> agrega archivos al siguiente estado
git commit -m "nombre_mensaje" -> agrega mensaje a archivo(s) preparados y futuro por confirmar
git push -> envia archivo(s) al repositorio remoto

# 2.0 - Proceso para eliminar archivo
git rm 'nombre_archivo'
git commit -m 'nombre_mensaje'
git push

-------------------------------------------------------------------

# 3.0 - Procesos para crear ramas - Dos Formas:
1 - git checkout -b 'nombre_rama'
2 - git branch 'nombre_rama' + git checkout 'nombre_rama'

# 4.0 - Proceso para borrar de forma local
git checkout 'otra_rama'
git branch -d 'nombre_rama' |  git branch -D 'nombre_rama'

# 5.0 - Proceso para borrar rama remota
git push origin --delete 'nombre_rama'

# 6.0 - Proceso para sincronizar rama local-remoto
git push -u origin 'nombre_rama'